{
  "index_type": "hnsw",
  "metric": "cosine",
  "parameters": {
    "M": 32,
    "ef_construction": 300,
    "ef_search": 60
  },
  "normalize": true,
  "description": "HNSW index optimized for FP16 AMP compensation (ef_search=60 for better recall with minimal latency cost)"
}
