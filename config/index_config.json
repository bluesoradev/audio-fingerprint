{
  "index_type": "hnsw",
  "metric": "cosine",
  "parameters": {
    "M": 32,
    "ef_construction": 300,
    "ef_search": 50
  },
  "normalize": true,
  "description": "HNSW index optimized for balanced performance (ef_search=50 for optimal recall/speed balance with GPU acceleration)"
}
